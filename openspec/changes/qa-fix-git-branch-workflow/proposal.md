# Proposal: Align git workflow with sprint guide (QA-15)

## Intent
SPRINT-3 guide requires `develop` branch and clean tree. Repo only has `main`. Either create develop flow or rewrite guides to main-based trunk development.

## Approach
Recommend lightweight: document `main` + short-lived feature branches; update sprint guides to match. Create `develop` only if team wants git-flow.
