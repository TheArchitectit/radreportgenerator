# Design: Guide correction
Compose methods take `(IContainer container, IReportData data, PdfGenerationOptions options)`.
Remove literal `\n` corruption in footer sample.
Add banner: this is a template for future Sprint 3 work, not current src/.
