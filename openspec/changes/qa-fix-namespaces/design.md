# Design: Namespace alignment

Replace `namespace LiveOptics` / `using LiveOptics` with OpenReportViewer equivalents.
Rename csproj files; keep project folder names as OpenReportViewer.*.
Update XAML x:Class and xmlns:vm paths.
